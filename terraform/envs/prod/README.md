# terraform/envs/prod

Stub. Not implemented — account ID not yet confirmed, and promotion to prod is a Section 12
checkpoint requiring the CTO's sign-off regardless of technical readiness. Copy the structure of
[`../dev/`](../dev/) only once that sign-off is in hand.
