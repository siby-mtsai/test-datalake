# Compaction runbook

TODO: document during Phase 3 (brief Section 4.2 — run Iceberg compaction, `OPTIMIZE` in Athena,
weekly on tables receiving many small files; Section 3, Phase 3 step 2 — `expire_snapshots` and
`remove_orphan_files` weekly).
