# .github/workflows

Phase 1. Expected workflows per brief Section 8: plan on PR, apply on merge, using OIDC to the
target account (no long-lived AWS keys in CI). Not yet implemented — blocked on AWS OIDC setup and
confirming the `DeveloperPowerUser` role's IAM permissions (Section 7, Phase 1 step 3).
